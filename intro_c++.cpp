// intro_c++.cpp
#include <cfloat>
#include <climits>
#include <ios>
#include <iostream>
#include <cmath>//math library/header
#include <vector>/**/
#include <vector>
using std::cout;//standard 
using std::cin;//standar
//function
/*double myPower(double base, double exp)
{
    double result=1;
    for (int i=0; i<exp; i++)
    {
        result=result*base;
    }
    return result;
}
//creating a function
int main() 
{
    I can write anything in between and 
    also i can write in more lines*/


    //int slices;//initialization of the variable
    //cin>>slices;
    //cout<<"You have "<<slices<<" slices."<<std::endl;
    //lets define our base and exponent
    /*int base, exponent;
    cout<<"what is the base:";
    cin>> base;
    cout<<"what is the exponent:";
    cin>> exponent;

   


    double power = pow(base, exponent);
    cout<< power;*/
    //double power=myPower(10,4);
    //cout<< power;*/
//int data type
/*int main(){
    short a; //int data types

    int x;
    long b;
    long long c;
    unsigned short d;
    unsigned long e;
    unsigned long long f;
    cout<<sizeof(long long);

    cout<<LLONG_MAX;



}*/
/*CHAR DATaTYPE
int main()
{
    unsigned char x = 131;//for char we have to use single quotes as double quotes ar