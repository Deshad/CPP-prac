#include<iostream>

/*Function Overload
class Position
{
    public:
    
    int x = 10;
    int y = 20;
    Position operator +(Position pos) // overloading the + operator
    {
        Position result;// creating a new object to store the result
        result.x = x + pos.x;// adding the x coordinates
        result.y = y + pos.y;// adding the y coordinates
        return result;
    }
    bool operator ==(Position pos) // overloading the == operator
    {
        if (x == pos.x && y == pos.y) // comparing the coordinates
        {
            return true;
        }
        return false;

    }
};


int main()
{
    Position pos1, pos2;// creating two objects of Position class
    Position pos3 = pos1 + pos2;// calling the overloaded + operator
    std::cout<<"Sum of coordinates: ("<<pos3.x<<", "<<pos3.y<<")"<<std::endl;// printing the result
    pos2.x=30;
    if (pos1 == pos2) // calling the overloaded == operator
    {
        std::cout<<"Coordinates are equal."<<std::endl;

    }
    else {
    {
        std::cout<<"Coordinates are not equal."<<std::endl;
    }
    }
    
    

return 0;

}

*/

